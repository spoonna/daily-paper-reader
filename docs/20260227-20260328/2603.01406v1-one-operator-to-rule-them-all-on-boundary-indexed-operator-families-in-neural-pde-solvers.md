---
title: One Operator to Rule Them All? On Boundary-Indexed Operator Families in Neural PDE Solvers
title_zh: 一个算子统治一切？论神经 PDE 求解器中由边界索引的算子族
authors: Lennon J. Shikhman
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01406v1"
tags: ["query:dl-pde"]
score: 8.0
evidence: 在不同边界条件下学习解算子的神经PDE求解器
tldr: 本研究挑战了神经偏微分方程（PDE）求解器学习单一通用算子的传统观点。作者指出，当边界条件变化时，模型实际上学习的是受训练边界分布约束的“边界索引算子族”。通过将算子学习形式化为边界条件下的条件风险最小化，研究揭示了模型在训练分布之外的不可识别性。实验证明，即使模型在力项或分辨率上具有泛化性，在边界条件偏移时仍会失效，强调了显式边界感知建模对PDE基础模型的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.01406v1/fig-001.webp\", \"caption\": \"Figure 3: Boundary extrapolation via increased Dirichlet bandwidth. Performance degrades monotonically as higher-frequency boundary components are introduced beyond the training support.\", \"page\": 8, \"index\": 1, \"width\": 582, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2603.01406v1/fig-002.webp\", \"caption\": \"Figure 2: Boundary extrapolation via Dirichlet mean shifts. Relative L2 error increases smoothly and symmetrically as boundary conditions move away from the training distribution, despite unchanged forcing distribution, resolution, and PDE operator.\", \"page\": 7, \"index\": 2, \"width\": 582, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2603.01406v1/fig-003.webp\", \"caption\": \"Figure 1: Training dynamics for boundary-aware Fourier Neural Operators trained on µB0 (left) and µB1 (right). Relative L2 error decreases smoothly and stabilizes in both cases, indicating that subsequent generalization failures are not due to optimization instability or lack of convergence.\", \"page\": 6, \"index\": 3, \"width\": 829, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2603.01406v1/fig-004.webp\", \"caption\": \"Figure 4: Conditional expectation behavior under boundary ablation. For a fixed forcing f∗, the boundary-ablated model output (left) closely matches the Monte Carlo estimate of E[u | f∗] (center), with the absolute difference shown on the right. This provides empirical evidence that training via empirical risk minimization leads to conditional averaging when boundary information is unavailable.\", \"page\": 9, \"index\": 4, \"width\": 748, \"height\": 242}]"
motivation: 探究神经PDE求解器在边界条件变化时是否真正学习到了通用的物理算子，以及其泛化能力的本质。
method: 将算子学习框架化为基于边界条件的条件风险最小化问题，并结合泊松方程进行受控实验分析。
result: 理论和实验均表明，模型性能高度依赖于训练时的边界分布，在边界条件偏移或缺失时会出现严重的泛化失效。
conclusion: 现有的神经PDE求解器存在边界泛化瓶颈，开发PDE基础模型必须引入显式的边界感知建模机制。
---

## 摘要
神经 PDE 求解器通常被描述为学习将问题数据映射到 PDE 解的解算子。在这项工作中，我们认为当边界条件发生变化时，这种解释通常是不正确的。我们表明，标准的神经算子训练隐式地学习了一个由边界索引的算子族，而不是单个与边界无关的算子，且所学习的映射从根本上取决于训练期间看到的边界条件分布。我们通过将算子学习框架化为基于边界条件的条件风险最小化来形式化这一观点，这导致了在训练边界分布支撑集之外的不可识别性结果。因此，在强迫项或分辨率上的泛化并不意味着跨边界条件的泛化。我们通过对泊松方程的受控实验支持了我们的理论分析，展示了在边界条件偏移下的剧烈性能下降、不同边界系之间的跨分布失效，以及在移除边界信息时向条件期望的收敛。我们的结果阐明了当前神经 PDE 求解器的一个核心局限性，并强调了在追求 PDE 基础模型时进行显式边界感知建模的必要性。

## Abstract
Neural PDE solvers are often described as learning solution operators that map problem data to PDE solutions. In this work, we argue that this interpretation is generally incorrect when boundary conditions vary. We show that standard neural operator training implicitly learns a boundary-indexed family of operators, rather than a single boundary-agnostic operator, with the learned mapping fundamentally conditioned on the boundary-condition distribution seen during training. We formalize this perspective by framing operator learning as conditional risk minimization over boundary conditions, which leads to a non-identifiability result outside the support of the training boundary distribution. As a consequence, generalization in forcing terms or resolution does not imply generalization across boundary conditions. We support our theoretical analysis with controlled experiments on the Poisson equation, demonstrating sharp degradation under boundary-condition shifts, cross-distribution failures between distinct boundary ensembles, and convergence to conditional expectations when boundary information is removed. Our results clarify a core limitation of current neural PDE solvers and highlight the need for explicit boundary-aware modeling in the pursuit of foundation models for PDEs.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：神经 PDE 求解器中的边界索引算子族研究

#### 1. 核心问题与研究动机
*   **核心问题**：当边界条件（Boundary Conditions, BCs）发生变化时，神经 PDE 求解器（如 FNO）究竟学习到了什么？是学习到了一个通用的、与边界无关的物理算子，还是仅仅拟合了训练集特定的边界分布？
*   **研究背景**：目前主流观点认为神经算子学习的是从问题数据到解的映射。但在经典 PDE 理论中，解算子是由微分方程和边界条件共同定义的。作者指出，现有模型往往隐式处理边界，这导致模型在面对未见过的边界分布时，其泛化能力存在根本性的结构限制。

#### 2. 方法论与核心思想
*   **核心思想**：将神经算子学习重新定义为**基于边界条件的条件风险最小化（Conditional Risk Minimization）**。
*   **关键技术细节**：
    *   **边界索引算子族**：作者认为模型学习的不是单一算子 $S$，而是由边界 $B$ 索引的一组算子族 $\{S_B\}$。
    *   **不可识别性（Non-identifiability）**：在经验风险最小化（ERM）框架下，模型仅在训练边界分布 $\mu_B$ 的支撑集内受到约束。在支撑集之外，存在无数个等效的映射函数能使训练损失最小，导致模型在边界偏移时表现具有随机性和不可预测性。
    *   **条件期望收敛**：理论推导表明，如果模型输入中缺失或弱化了边界信息，ERM 会驱动模型收敛于解在给定边界分布下的**条件期望** $\mathbb{E}_{B \sim \mu_B} [S(f, B) | f]$，而非任何特定边界下的真实解。

#### 3. 实验设计与 Benchmark
*   **实验场景**：二维泊松方程 $-\Delta u = f$，采用混合边界条件（左/下为 Dirichlet，右/上为 Neumann）。
*   **数据集生成**：
    *   **强迫项 $f$**：固定分布，由截断傅里叶级数生成。
    *   **边界分布 $\mu_{B0}$ 与 $\mu_{B1}$**：通过改变傅里叶系数的振幅、均值偏移和带宽来构造不同的边界分布。
*   **对比方法与设置**：
    *   **边界感知 FNO**：输入包含强迫项、坐标及显式的边界通道（数值和掩码）。
    *   **边界消融 FNO**：仅输入强迫项和坐标，不提供边界信息。
    *   **测试维度**：分布内测试、跨分布测试（在 $\mu_{B0}$ 训练，在 $\mu_{B1}$ 测试）、边界外推（均值偏移、频率增加）。

#### 4. 资源与算力
*   **硬件设备**：Dell Pro Max T2 工作站。
*   **核心组件**：Intel Core Ultra 9 285K 处理器，128 GB DDR5 ECC 内存。
*   **GPU**：**NVIDIA RTX PRO 6000 Blackwell GPU**（1块）。
*   **训练细节**：Adam 优化器，学习率 $8 \times 10^{-4}$，Batch Size 12，训练 2500 个梯度步。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   完成了跨分布泛化对比实验（Table 1）。
    *   进行了两类边界外推实验（均值偏移 sweep 和 频率带宽 sweep）。
    *   进行了边界消融与条件期望一致性的验证实验（Figure 4）。
*   **充分性评价**：实验设计非常**精准且受控**。通过固定微分算子和强迫项分布，仅改变边界分布，成功分离了变量。虽然仅针对泊松方程这一种 PDE，但对于验证其“不可识别性”和“边界索引”的核心理论假设是充分且客观的。

#### 6. 主要结论与发现
*   **边界依赖性**：模型性能高度依赖于训练时的边界分布。跨分布测试时，相对 $L_2$ 误差从 ~8% 剧增至 50%-60%。
*   **外推失效**：即使是微小的边界均值偏移或频率增加，都会导致误差平滑且对称地增长，证明模型并未真正掌握物理规律的边界不变性。
*   **平均效应**：不带边界信息的模型会退化为输出“平均解”，这在物理上是无效的。
*   **泛化误区**：在强迫项或分辨率上的泛化成功，并不代表模型在边界条件上具有鲁棒性。

#### 7. 优点
*   **理论深刻**：提供了一个清晰的数学框架（条件风险最小化）来解释神经 PDE 求解器的行为。
*   **实验纯净**：通过严格控制变量，排除了模型容量不足或优化失败的干扰，直击“分布偏移”这一本质问题。
*   **警示意义**：对当前追求“大而全”的 PDE 基础模型提出了重要的结构性质疑，具有很强的行业指导价值。

#### 8. 不足与局限
*   **PDE 覆盖面有限**：仅测试了线性、椭圆型的泊松方程，未涉及非线性、双曲型（如流体动力学）或随时间演化的 PDE。
*   **架构单一**：主要基于 FNO 进行实验，虽然理论具有通用性，但未在 DeepONet 或 Transformer-based 算子模型上进行大规模验证。
*   **未提供解决方案**：论文侧重于揭示问题（诊断性），而非提出新的边界感知架构（治疗性）。

（完）
