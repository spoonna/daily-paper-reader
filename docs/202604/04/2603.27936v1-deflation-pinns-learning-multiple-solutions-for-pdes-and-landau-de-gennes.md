---
title: "Deflation-PINNs: Learning Multiple Solutions for PDEs and Landau-de Gennes"
title_zh: Deflation-PINNs：学习偏微分方程与朗道-德热纳模型的多解
authors: "Sean Disarò, Ruma Rani Maity, Aras Bacho"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27936v1"
tags: ["query:dl-pde"]
score: 9.0
evidence: 用于求解非线性PDE的PINN和DeepONet
tldr: 针对物理信息神经网络（PINNs）难以求解非线性偏微分方程（PDEs）多解的问题，本文提出Deflation-PINNs框架。该方法将紧缩（deflation）损失函数与DeepONets架构相结合，通过在损失函数中引入紧缩项，强制模型系统性地寻找并收敛到不同的解分支。在具有复杂能量景观的Landau-de Gennes液晶模型上的实验证明，该方法能有效识别并表征多种不同的晶体结构，并具有理论收敛性保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27936v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture of the Deflation-PINN.\", \"page\": 5, \"index\": 1, \"width\": 829, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-002.webp\", \"caption\": \"Figure 2: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 9, \"index\": 2, \"width\": 660, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-003.webp\", \"caption\": \"Figure 3: Diagonally stable molecular alignments: (a) D1, (b) D2 states and rotated stable molecular alignments: (c) R1, (d) R2, (e) R3, (f) R4 states. See Maity et al. (2021a) for more details.\", \"page\": 15, \"index\": 3, \"width\": 694, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2603.27936v1/fig-004.webp\", \"caption\": \"Figure 4: Vector field plots of one diagonal and two rotated solutions, denoted by D1, R1 and R3 in suffix, with Deflation PINNs and FEM.\", \"page\": 16, \"index\": 4, \"width\": 660, \"height\": 500}]"
motivation: 传统的PINNs通常只能找到单一解，难以处理具有多个平衡态或解分支的复杂非线性物理系统。
method: 提出Deflation-PINNs框架，通过在损失函数中引入紧缩项来排斥已发现的解，从而引导模型探索并收敛至新的解分支。
result: 在Landau-de Gennes模型上的数值实验成功识别并刻画了多种不同的液晶结构，验证了该方法寻找多解的有效性。
conclusion: Deflation-PINNs为求解具有多解特性的非线性PDE问题提供了一种具备理论支撑且高效的新型数值手段。
---

## 摘要
非线性偏微分方程（PDEs）在数学物理和工程领域无处不在。尽管物理信息神经网络（PINNs）已成为求解偏微分方程问题的强大工具，但由于其设计初衷是每次寻找单个解，因此通常难以识别多个不同的解。为了解决这一局限性，我们引入了 Deflation-PINNs，这是一种将紧缩损失（deflation loss）与基于 PINNs 和深度算子网络（DeepONets）的架构相结合的新型框架。通过在损失函数中引入紧缩项，我们的方法系统地迫使 Deflation-PINN 寻找并收敛于多个不同的有限解分支。我们提供了关于模型收敛性的理论证据，并通过对液晶朗道-德热纳模型（该系统以其复杂的能量景观和多个平衡态而闻名）的数值实验证明了 Deflation-PINNs 的有效性。结果表明，Deflation-PINNs 能够成功识别并表征多种不同的晶体结构。

## Abstract
Nonlinear Partial Differential Equations (PDEs) are ubiquitous in mathematical physics and engineering. Although Physics-Informed Neural Networks (PINNs) have emerged as a powerful tool for solving PDE problems, they typically struggle to identify multiple distinct solutions, since they are designed to find one solution at a time. To address this limitation, we introduce Deflation-PINNs, a novel framework that integrates a deflation loss with an architecture based on PINNs and Deep Operator Networks (DeepONets). By incorporating a deflation term into the loss function, our method systematically forces the Deflation-PINN to seek and converge upon distinct finitely many solution branches. We provide theoretical evidence on the convergence of our model and demonstrate the efficacy of Deflation-PINNs through numerical experiments on the Landau-de Gennes model of liquid crystals, a system renowned for its complex energy landscape and multiple equilibrium states. Our results show that Deflation-PINNs can successfully identify and characterize multiple distinct crystal structures.

---

## 论文详细总结（自动生成）

### 论文总结：Deflation-PINNs：学习偏微分方程与朗道-德热纳模型的多解

#### 1. 核心问题与整体含义
非线性偏微分方程（PDEs）在物理和工程中常表现出**多解性**（如不同的平衡态或能量极小值点）。传统的物理信息神经网络（PINNs）通常被设计为寻找单一解，难以系统性地捕获所有可能的解分支。本文的核心动机是开发一种新型框架——**Deflation-PINNs**，旨在通过单一训练过程同时发现并区分非线性 PDE 的多个离散解，特别是在复杂的物理系统（如液晶的 Landau-de Gennes 模型）中。

#### 2. 方法论：核心思想与技术细节
Deflation-PINNs 结合了算子学习架构与一种特殊的“紧缩”机制：
*   **简化架构（Modified DeepONet）**：
    *   模型借鉴了 DeepONet 的结构，但为了效率将其简化。它包含一个处理空间坐标的 **Trunk Net** ($\tau$) 和一组可学习的 **Branch Weights** ($\beta_k$)。
    *   每个解分支 $k$ 由特定的特征向量 $\beta_k$ 表示，最终输出为 $u_k(x) = \sum \tau_i(x) \cdot \beta_{ki}$。这种设计允许模型以极少的参数增量同时表示 $K$ 个不同的函数。
*   **紧缩损失函数（Deflation Loss）**：
    *   为了防止多个分支收敛到同一个解，引入了紧缩项 $L_{Def}$。该项基于不同分支间的 $L_2$ 距离，当两个解的距离小于预设阈值 $d_{min}$ 时产生惩罚。
    *   总损失函数 = 物理信息损失（PIML Loss） + 紧缩损失（Deflation Loss）。
*   **硬约束边界条件**：
    *   引入了基于**径向边界距离函数（RBD function）**的构造方法，强制模型在架构层面精确满足 Dirichlet 边界条件，从而减少了损失函数中的目标数量，提高了训练稳定性。

#### 3. 实验设计
*   **场景与数据集**：研究聚焦于二维 **Landau-de Gennes (LdG)** 液晶模型。这是一个极具挑战性的基准，因为它具有复杂的能量景观，在正方形区域内存在 6 个已知的稳定平衡态。
*   **Benchmark（基准）**：对比对象是传统的**有限元方法（FEM）**。
*   **对比方法**：文中讨论了 HomPINNs（半监督）和基于随机初始化的 Ensemble PINNs，并指出 Deflation-PINNs 在避免冗余和系统性搜索方面的优势。

#### 4. 资源与算力
*   **硬件**：实验在一台配备 **NVIDIA RTX 3060-Ti GPU (8GB VRAM)** 的工作站上运行。
*   **训练细节**：
    *   使用 Adam 优化器训练了 **10,000 个 epoch**。
    *   学习率设为 $1 \times 10^{-3}$。
    *   Trunk Net 宽度为 4000，包含一个隐藏层，激活函数为 tanh。
    *   配置了 16 个分支权重（$p=16$）来表示 6 个解分支。

#### 5. 实验数量与充分性
*   **实验规模**：论文重点展示了针对 LdG 模型的深度案例研究。通过设置 $K=6$，模型成功识别了所有 6 个预期的物理状态（包括 2 个对角态 D1/D2 和 4 个旋转态 R1-R4）。
*   **客观性与公平性**：实验通过矢量场图直观对比了神经网络解与 FEM 参考解，结果显示两者高度吻合。虽然实验主要集中在一个核心物理模型上，但该模型本身的多解复杂性足以验证方法的有效性。

#### 6. 主要结论与发现
*   **多解捕获能力**：Deflation-PINNs 能够成功且稳定地识别出 LdG 模型中的所有 6 个稳定解，证明了紧缩机制在神经网络训练中的有效性。
*   **理论支撑**：论文提供了关于硬约束模型近似能力的理论证明（基于 Chen & Chen 的通用近似定理），确保了该架构在数学上的完备性。
*   **效率提升**：相比于训练多个独立 PINN 的集成方法，Deflation-PINNs 通过共享 Trunk Net 显著减少了参数量和计算冗余。

#### 7. 优点
*   **系统性**：不再依赖随机初始化来“碰运气”寻找多解，而是通过损失函数强制探索不同分支。
*   **硬约束集成**：通过 RBD 函数精确处理复杂几何形状的边界条件，提升了物理一致性。
*   **架构优雅**：将 DeepONet 简化为处理有限解分支的形式，既保留了算子学习的表达力，又降低了计算开销。

#### 8. 不足与局限
*   **超参数敏感性**：紧缩距离 $d_{min}$ 是一个关键的超参数，目前需要根据经验或先验知识调整。如果设置不当，可能导致物理损失无法收敛或无法区分相近的解。
*   **解的数量预设**：用户需要预先指定解的数量 $K$。如果 $K$ 小于实际存在的解数，会遗漏解；如果 $K$ 过大，可能会产生非物理的重复或无效解。
*   **泛化验证**：目前仅在 LdG 这一特定物理模型上进行了验证，对于更高维或具有连续解簇的 PDE 问题的适用性尚待进一步研究。

（完）
